# Netlify: connect the repo (or use the Deploy button in DEPLOY.md). Headers live in public/_headers.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
